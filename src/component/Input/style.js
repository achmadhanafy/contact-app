import { Color } from "../../config/Color";

export default {
  container: {
    borderColor: Color.neutral.light,
    borderWidth: 1,
    borderRadius: 24,
    paddingHorizontal: 16,
    flexDirection: 'row',
    alignItems: 'center',
  },
};
